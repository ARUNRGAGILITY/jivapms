from django.db import models

# Create your models here.
# User mgmt uses the default user model provided by Django, so no custom user model is defined here.
# The default user model is defined in django.contrib.auth.models.User.